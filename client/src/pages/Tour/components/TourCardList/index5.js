import React from 'react'
import { TourCardList } from './TourCardList'
import { data as card } from '../../data/index'

const TourApp = (p) => {
  const { card } = p
  return (
  <div>index</div>)
}

export default TourApp
